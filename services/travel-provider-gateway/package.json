{
  "name": "travel-provider-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "probe:contract": "npm run build && node scripts/probe-live-contract.mjs",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@fly-ai/flyai-cli": "^1.0.16",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0",
    "vitest": "^4.1.10"
  }
}
