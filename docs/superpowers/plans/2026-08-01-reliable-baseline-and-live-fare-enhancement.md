# Reliable Baseline And Live-Fare Enhancement Implementation Plan

**Status:** implementation and production acceptance complete on 2026-08-02. The final runtime is Chat primary with shadow off after a successful Responses switch and rollback. WeChat direct-open ICP classification and external alert routing remain operational residuals. No push is authorized.

1. [x] Record the verified production incident and approve the split between baseline city and live-fare schemes.
2. [x] Add failing FlyAI regressions for exact `飞机` / `火车` enums and preserve mixed-mode, segment-order, evidence, and diagnostic-redaction guards.
3. [x] Implement the exact enum allowlist and run focused gateway tests.
4. [x] Add a redacted live normalization canary plus patterned invalid-response circuit breaker.
5. [x] Add failing candidate/task tests for canonical Amap coordinates, deterministic baseline evidence, and candidate-first participant-interleaved order.
6. [x] Persist server-verified departure coordinates and the automatic run's baseline selection with a migration and guarded creation path.
7. [x] Render baseline city before live progress; keep it visible for incomplete/failed states without fares or scheme claims.
8. [x] Replace the hard-coded secondary-adapter flag with a real injected/configured capability and safe no-provider default.
9. [x] Run focused/full gateway and root gates plus guarded disposable-PostgreSQL migration, transaction, and permission behavior tests.
10. [x] Record local acceptance and list external provider procurement/configuration and production migration/deployment as operator work.
11. [x] Amend the authority spec with Chat Completions as stable default, Responses as a gated candidate, read-only shadow limits, rollout thresholds, and environment-only rollback.
12. [x] Add failing dual-transport tests for selection, strict JSON schema parity, 15-second timeout, retry/error mapping, DeepSeek Responses compatibility limits, and secret-safe logging.
13. [x] Implement provider-neutral Chat Completions and Responses transports plus fail-closed server-only configuration; preserve all deterministic agent/publication guards.
14. [x] Implement sampled/capped read-only shadow comparison and a credentialed production-like aggregate probe with no business-state writer.
15. [x] Run focused/full local gates and a 102-pair credentialed comparison. Responses cleared the predeclared comparison thresholds; production default remains Chat because current ECS configuration/deployment/rollback evidence is unavailable.
16. [x] Run the credentialed, redacted FlyAI flight/train contract canary; record it as sampled live-provider evidence rather than supplier-wide recovery.
17. [x] Audit the second-provider seam and available configuration; record the procurement/configuration checklist and acceptance gate without fabricating failover.
18. [x] Implement redacted schema-circuit/DeepSeek observations and document external coverage/divergence alert actions.
19. [x] Record implemented, production-verified, and external/manual boundaries in acceptance and progress documents.
20. [x] Obtain a restorable Supabase backup, reconcile the manually applied `202607280001` history gap, apply `202608010001`, and complete column/constraint/RPC/role/rollback postflight.
21. [x] Inspect ECS server-only model/gateway configuration, deploy the verified tree through the project Compose path, and pass authenticated gateway plus Chat/shadow/rollback smokes.
22. [x] Create fresh production plans and complete mainland-phone baseline/live/incomplete/failed, Amap coordinate, ordering, participant-evidence, retry, and transaction-replay acceptance. Evidence: `docs/acceptance/2026-08-02-reliable-baseline-production-acceptance.md`.
